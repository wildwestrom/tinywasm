[package]
name = "tinywasm-firmware"
edition.workspace = true

[dependencies]
tinywasm = { path = "../tinywasm", default-features = false, features = ["parser"] }
linked_list_allocator = { version = "0.10", default-features = false, features = ["const_mut_refs", "use_spin"] }
